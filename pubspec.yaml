name: restora_app
description: "A Restaurant Management System with Maroon and Cream Theme."
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  google_fonts: ^6.1.0
  intl: ^0.18.1
  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  # BAGIAN YANG DIPERBAIKI:
  uses-material-design: true

  assets:
    - assets/images/